{{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-sm btn-default float-right">
        <i class="fas fa-save"></i> Enregistrer
    </button>
{{ form_end(form) }}
